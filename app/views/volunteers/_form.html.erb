<%= form_with(model: [@event, volunteer]) do |form| %>
  <% if volunteer.errors.any? %>
    <div class="text-red-700">
      <h2><%= pluralize(volunteer.errors.count, "error") %> prohibited this volunteer from being saved:</h2>

      <ul>
        <% volunteer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :person, class: "block" %>
    <%= collection_select(:volunteer, :person_id, Person.all, :id, ->(person) { "#{person.name} (#{person.nickname})" }, {prompt: true}, {data: {controller: "searchable-select"}}) %>
  </div>

  <div>
    <%= form.label :event, class: "block" %>
    <%= collection_select(:volunteer, :event_id, Event.all, :id, :to_s, prompt: true) %>
  </div>

  <div>
    <%= form.label :role, class: "block" %>
    <%= form.text_field :role %>
  </div>

  <div>
    <%= form.submit class: "bg-nw5k-eggplant-500 hover:bg-nw5k-eggplant-700 text-nwk-eggplant-900 hover:text-white font-bold mt-2 py-2 px-4 rounded" %>
  </div>
<% end %>
