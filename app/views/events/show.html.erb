<div class="w-4/5 md:w-3/4 lg:w-2/3 xl:w-1/2 mx-auto">
  <div class="justify-center items-center bg-white mx-auto rounded-lg my-4 p-4 lg:p-16">
    <h1 class="text-3xl font-medium py-2"><%= "#{Event.model_name.human} #{@event.number} - #{l(@event.date)}" %></h1>
    <h2 class="text-xl font-medium py-2"><%= link_to t("nw5k.other_events"), events_path, class: "text-purple-600 no-underline hover:text-purple-800 hover:no-underline" %></h2>
    <table class="table-auto w-full my-4">
      <thead>
        <tr class="bg-purple-400 text-xl font-medium text-center">
          <th class="py-2 px-4 w-1/4"><%= t("nw5k.place") %></th>
          <th class="text-left py-2 px-4 w-1/2"><%= t("nw5k.participant") %></th>
          <th colspan="2" class="py-2 px-4 w-1/4"><%= t("nw5k.time") %></th>
        </tr>
      </thead>
      <tbody>
        <% @results.each_with_index do |result, index| %>
          <tr class="text-center text-xl hover:bg-purple-200 hover:text-purple-800">
            <td><%= link_to index + 1, result.person, class: "no-underline text-gray-800 hover:text-purple-800" %></td>
            <td class="text-left"><%= link_to result.person || t("nw5k.unknown"), result.person, class: "no-underline text-gray-800 hover:text-purple-800" %></td>
					    <td class="text-right"><%= link_to result.time_string, result.person, class: "no-underline text-gray-800 hover:text-purple-800" %></td>
            <td class="text-left w-4 lg:w-8">
						    <% if result.first_timer? %>
                <div class="has-tooltip">
                  <%= link_to "👋", result.person, class: "no-underline text-gray-800 hover:text-purple-800" %>
                  <span class="tooltip p-1 text-sm font-medium text-white bg-purple-900 rounded-md shadow-sm transition-opacity duration-300"><%= t("nw5k.first_timer") %></span>
                </div>
              <% elsif result.pb? %>
                <div class="has-tooltip">
                  <%= link_to "🔔", result.person, class: "no-underline text-gray-800 hover:text-purple-800" %>
                  <span class="tooltip p-1 text-sm font-medium text-white bg-purple-900 rounded-md shadow-sm transition-opacity duration-300"><%= t("nw5k.pb") %></span>
                </div>
              <% end %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
    <%= render "volunteers", volunteers: @volunteers if @volunteers.present? %>
  </div>
</div>
